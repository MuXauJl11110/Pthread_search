#include "kmp_search.h"

int main(int argc, char **argv) {
    //manage_threads(argc, argv);
    /*string s;
    s += "aabaa";

    vector<vector<size_t>> state = kmp_make(s);
    for (int i = 0; i < 128; i++) {
        for (int j = 0; j < s.size() + 1; j++) {
            cout << state[i][j] << " ";
        }
        cout << endl;
    }
    string text;
    text += "aabaabaaaabaabaaab";
    cout << pattern_check(state, text, s.size());*/
    return 0;
}